<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elramady.mechanic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mechanic">
        <activity android:name=".register.RegisterActivity">

        </activity>
        <activity android:name=".login.LoginActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        <activity android:name=".Mechanic.MechanicMainActivity" />
        <activity android:name=".User.requests.RequestsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User.details_mechanic.DetailsMechanicActivity" />
        <activity android:name=".User.home_page.HomePageActivity" />
        <activity android:name=".User.splash.SplashScreen" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>